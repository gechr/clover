// Package follow implements the follow provider: reuses another marker's resolved Candidate (via from=) from the registry and renders a value (version/commit/sha256). Omitting provider= defaults to follow.
package follow
